import styled from "@emotion/styled";
import SvgIcon, { SvgIconProps } from "@mui/material/SvgIcon";

const InteractionIcon = styled(SvgIcon)<SvgIconProps>``;

const Interaction = () => {};

export default Interaction;
