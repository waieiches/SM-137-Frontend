import styled from "@emotion/styled";

const HomeContainer = styled.div`
  background-color: #f0f0f0;
`;

const Home = () => {
  return (
    <HomeContainer>
      <h1>home</h1>
    </HomeContainer>
  );
};

export default Home;
